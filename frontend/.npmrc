# @jofr/capacitor-media-session declares a Capacitor 6 peer but its native code
# builds and runs fine on our Capacitor 8 (verified). Tolerate the stale peer
# range so `npm install` (local, Docker, CI) doesn't fail on ERESOLVE.
legacy-peer-deps=true
