# Loaded by `vite build --mode capacitor` (npm run build:mobile).
# Public HTTPS URL of the backend the native app talks to. The web build leaves
# this unset and uses same-origin relative paths instead.
#
VITE_API_BASE=https://ytdl.rafitolmg.dev
